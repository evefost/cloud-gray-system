package test.com.xie.gray.core;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 * GrayPredicate Tester.
 *
 * @author <Authors name>
 * @version 1.0
 * @since <pre>ʮ���� 10, 2018</pre>
 */
public class GrayPredicateTest {

    @Before
    public void before() throws Exception {
    }

    @After
    public void after() throws Exception {
    }

    /**
     * Method: apply(PredicateKey input)
     */
    @Test
    public void testApply() throws Exception {
//        GrayPredicate predicate = new GrayPredicate();
//        ServerInstanceStatus instanceStatus = ServerInstanceStatus.GRAY;
//        Constructor constructor = InstanceInfo.class.getConstructors()[0];
//        constructor.setAccessible(true);
//
//        InstanceInfo instanceInfo = (InstanceInfo) constructor.newInstance(new Object(){});
//        DiscoveryEnabledServer server = new DiscoveryEnabledServer(instanceInfo,false);
//        PredicateKey predicateKey = new PredicateKey(instanceStatus,server);
//        boolean apply = predicate.apply(predicateKey);
//        Assert.assertFalse(apply);
    }

//    public class  MyInstanceInfo extends InstanceInfo{
//
//        public MyInstanceInfo() throws IllegalAccessException, InstantiationException {
//            InstanceInfo.class.newInstance();
//            super();
//        }
//        public MyInstanceInfo(InstanceInfo ii) {
//            super(ii);
//        }
//    }

} 
